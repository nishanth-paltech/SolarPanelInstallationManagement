using Microsoft.AspNetCore.Identity;

namespace SolarPanelInstallationManagement.Models.Entities
{
    public class ApplicationUser : IdentityUser
    {
        // Keep empty for now
        // Extend later if needed (FirstName, LastName, etc.)
    }
}
